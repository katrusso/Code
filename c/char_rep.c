/*internal representation of characters*/
/*A is represented by the integer 65 */

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
  int letter = 'A';

  printf ("the internal representation of as a character constant in the ASCII set is: %d\n", letter);

  return EXIT_SUCCESS;
}
